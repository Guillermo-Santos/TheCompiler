<ContentDialog
    x:Class="Forge.Views.NewProjectContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Forge.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    PrimaryButtonText="Create"
    PrimaryButtonCommand="{x:Bind ViewModel.CreateCommand}"
    SecondaryButtonText="Cancel"
    DefaultButton="Primary"
    Title="New Project">

    <StackPanel Width="auto">
        <TextBlock Text="Project name:"/>
        <TextBox x:Name="ProjectName" PlaceholderText="Project Name" Text="{x:Bind ViewModel.ProjectName, Mode=TwoWay}"/>
        <StackPanel Orientation="Horizontal" Padding="2">
            <StackPanel>
                <TextBlock Text="Project path:"/>
                <TextBox x:Name="ProjectPath" 
                     Text="{x:Bind ViewModel.Path,Mode=OneWay}"
                     Width="370" 
                     MaxWidth="370"/>
            </StackPanel>
            <Button Content="Browse" Width="68" Command="{x:Bind ViewModel.BrowseCommand}"/>
        </StackPanel>
    </StackPanel>
</ContentDialog>